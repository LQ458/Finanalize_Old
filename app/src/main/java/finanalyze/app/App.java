/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package finanalyze.app;

import finanalyze.list.LinkedList;

import static finanalyze.utilities.StringUtils.join;
import static finanalyze.utilities.StringUtils.split;
import static finanalyze.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
